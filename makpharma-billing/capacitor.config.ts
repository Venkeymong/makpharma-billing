import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.makpharma.app',
  appName: 'makpharma',
  webDir: 'dist/makpharma-billing/browser'
};

export default config;